import {createContext} from "react";

// context 생성

// Todos 데이터 제공용 context
export const TodoContext = createContext();

// Todos Dispatch 제공용 context
// export const TodoDispatchContext = createContext();
